/**
 * next-sitemap config
 * Update `siteUrl` to your production domain before generating.
 */
module.exports = {
  siteUrl: 'https://www.rdimoveisdf.com',
  generateRobotsTxt: true,
  changefreq: 'daily',
  priority: 0.7,
  sitemapSize: 7000,
}
